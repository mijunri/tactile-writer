# 墨写 × Tactile 新加坡生产环境（cloudagentlab.com）
# 复制为 tactile-prod.env 后执行: python3 scripts/bootstrap-tactile-prod.py

TACTILE_API_BASE=https://cloudagentlab.com/api

# 墨写服务账号（在 Tactile 生产环境注册）
TACTILE_SERVICE_EMAIL=moxie-writer@imjson.cn
TACTILE_SERVICE_PASSWORD=MoxieWriter2026!
TACTILE_SERVICE_DISPLAY_NAME=墨写服务

# 工作空间与 Agent（bootstrap 后写入 state.json）
TACTILE_WORKSPACE_NAME=墨写空间
TACTILE_AGENT_NAME=通用写作助手

# 墨写后端地址（Skill 上传文章用，Agent 环境变量注入）
MOXIE_API_BASE=http://118.31.57.25/writer/api
MOXIE_UPLOAD_TOKEN=change-me-upload-token
